version: "3.4"

services:
  unbound:
    build: .
    restart: always
    ports:
      - "${EXTERNALIP}:53:53"
      - "${EXTERNALIP}:53:53/udp"
      - "127.0.0.1:53:53"
      - "127.0.0.1:53:53/udp"
        #    network_mode: "host"
        #    privileged: true
